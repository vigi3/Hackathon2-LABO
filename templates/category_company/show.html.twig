{% extends 'base.html.twig' %}

{% block title %}Catégorie{% endblock %}

{% block body %}
    <h1>Catégorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ category_company.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('category_company_edit', {'id': category_company.id}) }}" role="button">modifier</a>

    {{ include('category_company/_delete_form.html.twig') }}

    <a class="btn btn-primary" href="{{ path('category_company_index') }}" role="button">Retour à la liste</a>

{% endblock %}
