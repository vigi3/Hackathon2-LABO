{% extends 'base.html.twig' %}

{% block title %}CategoryCompany{% endblock %}

{% block body %}
    <h1>CategoryCompany</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_company.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_company.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_company_index') }}">back to list</a>

    <a href="{{ path('category_company_edit', {'id': category_company.id}) }}">edit</a>

    {{ include('category_company/_delete_form.html.twig') }}
{% endblock %}
