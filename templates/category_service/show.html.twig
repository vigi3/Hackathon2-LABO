{% extends 'base.html.twig' %}

{% block title %}CategoryService{% endblock %}

{% block body %}
    <h1>CategoryService</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_service.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_service.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_service_index') }}">back to list</a>

    <a href="{{ path('category_service_edit', {'id': category_service.id}) }}">edit</a>

    {{ include('category_service/_delete_form.html.twig') }}
{% endblock %}
