{% extends 'base.html.twig' %}

{% block title %}Modifier un service{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier un service</h1>
        <div class="row">
            <div class="col-12">
                {{ include('admin/service/_form.html.twig', {'button_label': 'Changer'}) }}
                {{ include('admin/service/_delete_form.html.twig') }}
                <a class="btn btn-danger" href="{{ path('service_admin_index') }}" role="button">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}
