{% extends 'base.html.twig' %}

{% block title %}Nouveau service{% endblock %}

{% block body %}
    <div class="container">
        <h1>Create nouveau service</h1>
        <div class="row">
            <div class="col-12">
                {{ include('admin/service/_form.html.twig') }}
                <a class="btn btn-primary" href="{{ path('service_admin_index') }}" role="button">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}
