{% extends 'base.html.twig' %}

{% block title %}Entreprise{% endblock %}

{% block body %}
    <div class="container">
        <h1>Entreprise</h1>
        <div class="row">
            <div class="col-12">
                <table class="table">
                    <tbody>
                    <tr>
                        <th>CompanyName</th>
                        <td>{{ company.companyName }}</td>
                    </tr>
                    <tr>
                        <th>ManagerName</th>
                        <td>{{ company.managerName }}</td>
                    </tr>
                    <tr>
                        <th>CompanyType</th>
                        <td>{{ company.companyType }}</td>
                    </tr>
                    <tr>
                        <th>Mail</th>
                        <td>{{ company.mail }}</td>
                    </tr>
                    <tr>
                        <th>Comment</th>
                        <td>{{ company.comment }}</td>
                    </tr>
                    </tbody>
                </table>
                <a class="btn btn-primary" href="{{ path('company_admin_edit', {'id': company.id}) }}" role="button">modifier</a>
                {{ include('admin/company/_delete_form.html.twig') }}
                <a class="btn btn-primary" href="{{ path('company_admin_index') }}" role="button">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}
