{% extends 'base.html.twig' %}

{% block title %}Nouvelle entreprise{% endblock %}

{% block body %}
    <div class="container">
        <h1>Nouvelle entreprise</h1>
        <div class="row">
            <div class="col-12">
                {{ include('admin/company/_form.html.twig') }}
            </div>
        </div>
    </div>
    <a class="btn btn-danger" href="{{ path('company_admin_index') }}" role="button">Retour à la liste</a>
{% endblock %}
