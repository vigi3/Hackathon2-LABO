{% extends 'base.html.twig' %}

{% block title %}Nouvelle catégorie{% endblock %}

{% block body %}
    <div class="container">
        <h1>Créer une nouvelle catégorie</h1>
        <div class="row">
            <div class="col-12">
                {{ include('admin/category_company/_form.html.twig') }}
            </div>
        </div>
    </div>
    <a class="btn btn-danger" href="{{ path('category_company_admin_index') }}" role="button">Retour à la liste</a>
{% endblock %}
