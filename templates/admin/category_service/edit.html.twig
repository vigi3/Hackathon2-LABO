{% extends 'base.html.twig' %}

{% block title %}Modifier la catégorie{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier la catégorie</h1>
        <div class="row">
            <div class="col-12">
                {{ include('admin/category_service/_form.html.twig', {'button_label': 'Changer'}) }}
                {{ include('admin/category_service/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
    <a class="btn btn-danger" href="{{ path('category_service_admin_index') }}" role="button">Retour à la liste</a>
{% endblock %}
