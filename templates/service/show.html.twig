{% extends 'base.html.twig' %}

{% block title %}Service{% endblock %}

{% block body %}
    <h1>Service</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Titre</th>
            <td>{{ service.title }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ service.description }}</td>
        </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('service_edit', {'id': service.id}) }}" role="button">modifier</a>

    {{ include('service/_delete_form.html.twig') }}

    <a class="btn btn-primary" href="{{ path('service_index') }}" role="button">Retour à la liste</a>

{% endblock %}
